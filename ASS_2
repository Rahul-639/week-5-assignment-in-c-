#include <bits/stdc++.h>
using namespace std;
struct StackNode {
    int data;
    StackNode *next;
    StackNode(int x) : data(x), next(NULL) {}
};
class MyStack {
  private:
    StackNode *top;
  public:
    MyStack() { top = NULL; }
    void push(int x) {
        StackNode *newNode = new StackNode(x);
        newNode->next = top; 
        top = newNode;        
    }
    int pop() {
        if (top == NULL)  
            return -1;
        int popped = top->data;
        StackNode *temp = top;
        top = top->next; 
        delete temp;      
        return popped;
    }
};
int main() {
    vector<vector<int>> queries = {{1,2}, {1,3}, {2}, {1,4}, {2}};
    MyStack st;
    vector<int> result;
    for (auto &q : queries) {
        if (q[0] == 1) {      
            st.push(q[1]);
        } else if (q[0] == 2) { 
            result.push_back(st.pop());
        }
    }
    for (int x : result) cout << x << " ";
    cout << endl;

    return 0;
}
